{
  "name": "sistema-tickets-spa",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "auth-db": "json-server --watch auth-db.json --port 3001",
    "data-db": "json-server --watch data-db.json --port 3002"
  },
  "dependencies": {
    "json-server": "^1.0.0" 
  }
}
